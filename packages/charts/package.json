{
	"name": "@basemark/charts",
	"version": "0.0.0",
	"private": true,
	"description": "Chart/plot components (ECharts) — a separate package per ARCHITECTURE.md §7's flag on heavy general-purpose dependencies",
	"license": "MIT",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"@basemark/core": "workspace:*",
		"echarts": "^6.1.0"
	},
	"devDependencies": {
		"@basemark/tsconfig-base": "workspace:*"
	}
}
