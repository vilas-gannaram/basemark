import type { ComponentRegistry } from '@basemark/core';

// STUB: not implemented. Do not build out until @basemark/core's directive
// contract is validated end-to-end (see experiments/).
export function registerChemComponents(_registry: ComponentRegistry): void {}
