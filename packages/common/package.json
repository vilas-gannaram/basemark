{
	"name": "@basemark/common",
	"version": "0.0.0",
	"private": true,
	"description": "Common domain components — tables, charts, maps, katex",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"@basemark/core": "workspace:*"
	},
	"devDependencies": {
		"@basemark/tsconfig-base": "workspace:*"
	}
}
